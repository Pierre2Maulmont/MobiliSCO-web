document.addEventListener('DOMContentLoaded', event => {
    // ici le DOM est chargé, on peut utiliser document.querySelector()
})

// ici le DOM n'est pas encore chargé, on ne peut pas utiliser document.querySelector()